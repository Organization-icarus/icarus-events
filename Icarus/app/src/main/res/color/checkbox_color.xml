<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Checked -->
    <item android:color="@color/accent_first" android:state_checked="true"/>

    <!-- Unchecked -->
    <item android:color="@color/lightText"/>

</selector>